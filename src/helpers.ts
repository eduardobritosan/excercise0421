/* eslint-disable no-unused-vars */
/**
 * Tipo de callback para map
 */
type MapCallback = (n: number[]) => number[];

/**
 * Tipo de callback para reduce
 */
type ReduceCallback = (n: number[]) => number;
